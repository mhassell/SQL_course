# can also highlight and click run in the GUI to run only one of these

# keywords are not case sensitive
# columns are case sensitive

SELECT <column> 
FROM <table>

SELECT <column1>, <column2> 
FROM <table>

SELECT * 	# all columns
FROM <table>   

SELECT DISTINCT job FROM emp;   # only unique elements of the job column

SELECT job FROM emp; 			# not the unique ones